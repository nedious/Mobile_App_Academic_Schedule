<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TermV30"
        tools:targetApi="31">

        <receiver
            android:name=".helper.MyReceiver"
            android:enabled="true"
            android:exported="true"></receiver>

        <activity
            android:name=".ui.ActivityAssessmentDetail"
            android:label="Assessment"
            android:parentActivityName=".ui.ActivityCourseDetail" />

        <activity
            android:name=".ui.ActivityCourseDetail"
            android:label="Course and Assessments"
            android:parentActivityName=".ui.ActivityTermDetail" />

        <activity
            android:name=".ui.ActivityTermDetail"
            android:label="Term and Courses"
            android:parentActivityName=".ui.ActivityTermHome" />

        <activity
            android:name=".ui.ActivityTermHome"
            android:label="Terms"
            android:parentActivityName=".ui.MainActivity" />

        <activity android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>